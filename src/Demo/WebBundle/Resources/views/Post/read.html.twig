<div class="well">
    <h4>阅读排行</h4>
    <div class="row">
        <div class="col-lg-12">
            <ul class="list-unstyled">
                {% for item in posts %}
                    <li><a href="{{ url('post_postInfo', {'id': item.getId}) }}">{{ item.getPostTitle }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <!-- /.row -->
</div>